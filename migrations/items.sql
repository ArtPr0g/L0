create table items
(
    item_uuid uuid,
    chrt_id integer,
    track_number varchar(255),
    price integer,
    rid varchar(255),
    name varchar(255),
    sale integer,
    size varchar(255),
    total_price integer,
    nm_id integer,
    brand varchar(255),
    status integer
);